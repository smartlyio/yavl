# Publish workflow — automated versioning and npm publishing.
#
# Contributors only need to add a label (patch/minor/major) to their PR.
# After merge, this workflow:
#   1. Determines the version bump type from merged PR labels
#   2. Bumps all package versions with lerna version (fixed mode)
#   3. Commits the version bump directly to master
#   4. Publishes each package to npm (see .github/scripts/publish-packages.sh)
#   5. Creates and pushes a git tag
#
# Publishing uses `npm publish` directly (via a separate script) rather
# than `lerna publish`, which has known issues with Yarn's registry proxy
# that cause E404 errors during publish.
#
# The default GITHUB_TOKEN cannot push to protected branches. This workflow
# uses the "smartlyio" GitHub App to generate a short-lived token that has
# permission to bypass branch protection on master. The app must be:
#   - Installed on this repository with Contents: Read & Write permission
#   - Listed in the master branch protection rule under "Allow specified
#     actors to bypass required pull requests"
# Required secrets: RELEASE_APP_ID, RELEASE_APP_PRIVATE_KEY, NPM_TOKEN.

name: Publish

on:
  push:
    branches: [master]

# Prevent concurrent runs. If multiple PRs are merged in quick succession,
# each triggered run will queue and execute in order, ensuring version bumps
# don't conflict with each other.
concurrency: ${{ github.workflow }}-${{ github.ref }}

jobs:
  publish:
    runs-on: ubuntu-latest

    # contents:write is needed for the default GITHUB_TOKEN (used by gh CLI).
    # The actual push to master uses the GitHub App token generated below.
    permissions:
      contents: write

    steps:
      # Generate a short-lived token from the "smartlyio" GitHub App.
      # This token can bypass branch protection rules on master, unlike
      # the default GITHUB_TOKEN which is intentionally blocked by GitHub.
      # The token expires after ~1 hour and is scoped to this repository only.
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}

      # Checkout using the app token so that subsequent git push commands
      # authenticate as the GitHub App (which has bypass permissions).
      - uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}
          # Full history is required to find the last "chore: version packages"
          # commit, which serves as the reference point for determining which
          # merged PRs still need to be included in a release.
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20.x

      - name: Install yarn
        run: npm install -g yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Build
        run: yarn build

      # Determine bump type from merged PR labels. Finds the last release
      # commit, queries for PRs merged after it, and picks the highest bump
      # (major > minor > patch). PR titles are saved for the commit message.
      # If no qualifying PRs are found, bump is empty and later steps are skipped.
      - name: Determine version bump from merged PRs
        id: bump
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./.github/scripts/determine-bump.sh

      # Bump all packages to the same new version (fixed mode).
      # --force-publish ensures every package is bumped even without code changes.
      # We handle git commit/tag ourselves so we can add [skip ci].
      # After lerna updates packages/* and lerna.json, we sync the root
      # package.json version (lerna skips it because it's private).
      - name: Version packages
        if: steps.bump.outputs.bump != ''
        run: |
          yarn lerna version ${{ steps.bump.outputs.bump }} \
            --yes \
            --force-publish \
            --no-git-tag-version \
            --no-push
          node -e "
            const fs = require('fs');
            const ver = require('./lerna.json').version;
            const pkg = require('./package.json');
            pkg.version = ver;
            fs.writeFileSync('package.json', JSON.stringify(pkg, null, 2) + '\n');
          "

      # Commit version bumps to master with [skip ci] to prevent infinite loops.
      # PR titles are included as a lightweight release log. The push
      # authenticates as the GitHub App (bypass branch protection).
      - name: Commit and tag
        if: steps.bump.outputs.bump != ''
        run: |
          VERSION=$(node -p "require('./lerna.json').version")

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A

          BODY=""
          if [ -f /tmp/pr-entries.txt ]; then
            BODY=$(cat /tmp/pr-entries.txt)
          fi

          git commit -m "chore: version packages v${VERSION} [skip ci]" -m "$BODY"
          git tag -f "v${VERSION}"
          git push
          git push --tags --force

      # Write ~/.npmrc with the npm auth token. The token value is resolved
      # at publish time from the NODE_AUTH_TOKEN env var set in the next step.
      - name: Log in to NPM registry
        uses: smartlyio/setup-npm-publish-action@v1
        env:
          AUTH_TOKEN_STRING: |
            //registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}
            always-auth = true
        with:
          npmrc-path: ${{ github.workspace }}/.npmrc

      # Publish each package to npm. The script is idempotent — versions
      # already on the registry are skipped, so re-running after a partial
      # failure is safe. See .github/scripts/publish-packages.sh.
      - name: Publish to npm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          GITHUB_WORKSPACE: ${{ github.workspace }}
        run: ./.github/scripts/publish-packages.sh
